\section{Phương pháp giảm chiều}

Chương trình bày nội dung chương giảm chiều trong \cite{mohri2018foundations}: PCA, KPCA, học manifold (Isomap, Laplacian eigenmaps, LLE) và bổ đề Johnson--Lindenstrauss. Nhóm bổ sung thêm mục \textbf{[MỞ RỘNG]} về trực quan hóa phi tuyến (t-SNE, UMAP) theo các bài báo gốc \cite{vandermaaten2008tsne,mcinnes2018umap}---hai phương pháp này \emph{không} có trong sách.

\subsection{Phân tích thành phần chính (PCA)}
\label{sec:pca}

Cho $k \in \{1,\ldots,N\}$ và $\mathbf{X}$ đã căn giữa. PCA tìm ma trận chiếu $\mathbf{P} \in P_k$ sao cho tái tạo sau chiếu gần $\mathbf{X}$ nhất theo chuẩn Frobenius:
\begin{equation}
\min_{\mathbf{P} \in P_k} \|\mathbf{P}\mathbf{X} - \mathbf{X}\|_F^2.
\label{eq:pca-objective}
\end{equation}

\begin{theorem}[Nghiệm PCA]
\label{thm:pca}
Nghiệm $\mathbf{P}^*$ của \eqref{eq:pca-objective} có dạng $\mathbf{P}^* = \mathbf{U}_k\mathbf{U}_k^\top$, trong đó $\mathbf{U}_k \in \R^{N \times k}$ gồm $k$ vector riêng ứng với $k$ trị riêng lớn nhất của $\mathbf{C} = \frac{1}{m}\mathbf{X}\mathbf{X}^\top$. Biểu diễn $k$ chiều là $\mathbf{Y} = \mathbf{U}_k^\top \mathbf{X}$.
\end{theorem}

\begin{proof}
Vì $\mathbf{P}$ là phép chiếu trực giao nên $\mathbf{P}^\top = \mathbf{P}$ và $\mathbf{P}^2 = \mathbf{P}$. Khai triển norm và dùng tính tuyến tính của trace:
\[
\|\mathbf{P}\mathbf{X} - \mathbf{X}\|_F^2
= \Tr\!\bigl[\mathbf{X}^\top\mathbf{P}^\top\mathbf{P}\mathbf{X} - 2\mathbf{X}^\top\mathbf{P}\mathbf{X} + \mathbf{X}^\top\mathbf{X}\bigr]
= -\Tr(\mathbf{X}^\top\mathbf{P}\mathbf{X}) + \Tr(\mathbf{X}^\top\mathbf{X}),
\]
số hạng thứ hai không phụ thuộc $\mathbf{P}$. Vậy ta cần cực đại $\Tr(\mathbf{X}^\top\mathbf{P}\mathbf{X})$ trên $P_k$.

Viết $\mathbf{P} = \mathbf{U}\mathbf{U}^\top$ với $\mathbf{U}$ có $k$ cột trực chuẩn. Hoán vị trace cho
\[
\Tr(\mathbf{X}^\top\mathbf{P}\mathbf{X})
= \Tr(\mathbf{U}^\top\mathbf{X}\mathbf{X}^\top\mathbf{U})
= \sum_{i=1}^k \mathbf{u}_i^\top \mathbf{X}\mathbf{X}^\top \mathbf{u}_i,
\]
với $\mathbf{u}_i$ là cột thứ $i$ của $\mathbf{U}$. Theo định lý Rayleigh--Ritz/Ky Fan, tổng trên đạt cực đại bằng tổng $k$ trị riêng lớn nhất của $\mathbf{X}\mathbf{X}^\top$, khi các cột của $\mathbf{U}$ là $k$ vector riêng tương ứng. Vì $\mathbf{C}=\frac{1}{m}\mathbf{X}\mathbf{X}^\top$ chỉ khác $\mathbf{X}\mathbf{X}^\top$ một hệ số dương $1/m$, các vector riêng tối ưu cũng chính là $k$ vector riêng ứng với $k$ trị riêng lớn nhất của $\mathbf{C}$. Suy ra
\[
\mathbf{P}^* = \mathbf{U}_k\mathbf{U}_k^\top,\qquad \mathbf{Y}=\mathbf{U}_k^\top \mathbf{X}.
\]
\end{proof}

\paragraph{Cách nhìn theo phương sai.}
Vector riêng của $\mathbf{C}$ trỏ theo hướng biến thiên mạnh nhất; trị riêng tương ứng bằng phương sai theo hướng đó. Thành phần chính thứ $i$ là chiếu lên hướng phương sai lớn thứ $i$, vuông góc các hướng trước.

\begin{extension}
Với $k=1$, PCA chọn hướng đơn vị $\mathbf{u}$ tối đa $\mathbf{u}^\top\mathbf{C}\mathbf{u}$---trùng vector riêng hàng đầu sau khi căn giữa dữ liệu.
\end{extension}

\begin{lemma}[Phương sai giữ lại và lỗi tái tạo]
\label{lem:pca-error}
Gọi $\gamma_1 \geq \cdots \geq \gamma_N \geq 0$ là các trị riêng của
$\mathbf{C}$. PCA bậc $k$ giữ tổng phương sai
$\sum_{i=1}^k\gamma_i$ và đạt lỗi tối thiểu
\[
  \|\mathbf{P}^*\mathbf{X}-\mathbf{X}\|_F^2
  = m\sum_{i=k+1}^{N}\gamma_i.
\]
Tỉ lệ phương sai giải thích bởi $k$ thành phần là
$\big(\sum_{i=1}^k\gamma_i\big)/\big(\sum_{i=1}^N\gamma_i\big)$.
\end{lemma}

\begin{proof}
Từ chứng minh Định lý~\ref{thm:pca},
$\Tr(\mathbf{X}^\top\mathbf{P}^*\mathbf{X})
=m\sum_{i=1}^k\gamma_i$. Mặt khác,
$\Tr(\mathbf{X}^\top\mathbf{X})=m\Tr(\mathbf{C})
=m\sum_{i=1}^N\gamma_i$. Thế vào \eqref{eq:pca-objective} cho biểu thức
lỗi. Phần phương sai được giữ chính là tổng trị riêng trên các hướng
được chiếu.
\end{proof}

\begin{extension}
Nếu hai trị riêng liên tiếp thỏa $\gamma_k = \gamma_{k+1}$ thì không gian con tối ưu không duy nhất: có thể chọn bất kỳ cơ sở trực chuẩn phù hợp trong không gian riêng đồng trị. Tuy vậy, giá trị lỗi tối ưu trong Bổ đề~\ref{lem:pca-error} không đổi.
\end{extension}

\begin{example}[Hai đặc trưng tương quan]
Đo cùng một đại lượng (ví dụ cỡ giày) bằng hai thang đơn vị khác nhau. Sau căn giữa, đám điểm gần một đường thẳng; chiếu lên thành phần chính thứ nhất cho một số gần đủ mô tả biến thiên (Hình~\ref{fig:pca-shoe}, Chương~2).
\end{example}

\paragraph{Thuật toán PCA từ đầu.}
Trước hết trừ vector trung bình khỏi mỗi mẫu để tạo $\mathbf{X}$ đã căn giữa; tiếp theo tính $\mathbf{C} = \frac{1}{m}\mathbf{X}\mathbf{X}^\top$ hoặc thực hiện SVD trực tiếp trên $\mathbf{X}$; sau đó chọn $k$ vector riêng lớn nhất làm các cột của $\mathbf{U}_k$; cuối cùng xuất embedding $\mathbf{Y} = \mathbf{U}_k^\top\mathbf{X}$. Dữ liệu xấp xỉ trong không gian gốc là $\hat{\mathbf{X}} = \mathbf{U}_k\mathbf{Y}$. Hiện thực của nhóm dùng thin SVD, do đó nhận $k \leq \min(m, N)$; giới hạn này phù hợp với bài toán giảm chiều đang xét.

\subsection{PCA trong không gian kernel (KPCA)}
\label{sec:kpca}

PCA giả định vector đặc trưng nằm trong $\R^N$. Nhiều khi ta chỉ có thể tính tương tự trong không gian $\mathcal{H}$ cao (hoặc vô hạn) chiều qua kernel $K(x,x') = \langle \Phi(x), \Phi(x')\rangle$. KPCA làm PCA trên ma trận Gram $\mathbf{K}$ thay vì $\mathbf{X}$ trực tiếp.

\paragraph{Căn giữa trong không gian đặc trưng.}
Ngay cả khi các $x_i$ đã căn giữa trong không gian đầu vào, ảnh
$\Phi(x_i)$ nhìn chung chưa có trung bình bằng không. Đặt
$\overline{\Phi}=\frac{1}{m}\sum_i\Phi(x_i)$,
$\widetilde{\Phi}(x_i)=\Phi(x_i)-\overline{\Phi}$ và
$\mathbf{H}=\mathbf{I}_m-\frac{1}{m}\mathbf{1}\mathbf{1}^\top$.
Ma trận Gram của các ảnh đã căn giữa là
\begin{equation}
  \widetilde{\mathbf{K}}=\mathbf{H}\mathbf{K}\mathbf{H},
  \qquad K_{ij}=K(x_i,x_j).
  \label{eq:kpca-centered-kernel}
\end{equation}
Với điểm mới $x$, đặt
$\mathbf{k}_x=(K(x_1,x),\ldots,K(x_m,x))^\top$. Vector tích vô hướng
đã căn giữa giữa các điểm huấn luyện và $x$ là
\begin{equation}
 \widetilde{\mathbf{k}}_x
 =\mathbf{k}_x-\frac{1}{m}\mathbf{K}\mathbf{1}
 -\frac{1}{m}\mathbf{1}\mathbf{1}^\top\mathbf{k}_x
 +\frac{1}{m^2}\mathbf{1}\mathbf{1}^\top\mathbf{K}\mathbf{1}.
 \label{eq:kpca-center-new}
\end{equation}

\paragraph{Các bước thực hiện.}
\begin{enumerate}
  \item Lập $\mathbf{K}$, $K_{ij} = K(x_i, x_j)$.
  \item Căn giữa kernel theo \eqref{eq:kpca-centered-kernel}.
  \item Lấy $k$ cặp trị riêng/vector riêng lớn nhất của $\tilde{\mathbf{K}}$.
  \item Với điểm mới $x$, căn giữa $\mathbf{k}_x$ theo \eqref{eq:kpca-center-new}. Tọa độ thứ $j$ của $x$ trên thành phần KPCA thứ $j$ là
  \[
  y_j(x)=\frac{\widetilde{\mathbf{k}}_x^\top \mathbf{v}_j}{\sqrt{\lambda_j}}.
  \]
\end{enumerate}

\begin{theorem}[Tọa độ KPCA]
\label{thm:kpca-coord}
Giả sử $\widetilde{\mathbf{K}}\mathbf{v}_j = \lambda_j \mathbf{v}_j$, $\lambda_j > 0$ và $\|\mathbf{v}_j\| = 1$. Hướng thành phần chính đơn vị thứ $j$ trong $\mathcal{H}$ có thể viết
\[
\mathbf{w}_j = \frac{1}{\sqrt{\lambda_j}}\sum_{i=1}^m v_{ji}\,\widetilde{\Phi}(x_i).
\]
Tọa độ của điểm $x$ lên hướng này bằng
\[
y_j(x) = \langle\widetilde{\Phi}(x),\,\mathbf{w}_j\rangle_{\mathcal{H}}
= \frac{\widetilde{\mathbf{k}}_x^\top \mathbf{v}_j}{\sqrt{\lambda_j}}.
\]
Với tập huấn luyện, embedding $k$ chiều có dạng
\[
\mathbf{Y} = \boldsymbol{\Lambda}_k^{1/2}\mathbf{V}_k^\top,
\]
trong đó $\boldsymbol{\Lambda}_k = \mathrm{diag}(\lambda_1,\ldots,\lambda_k)$.
\end{theorem}

\begin{proof}
Chuẩn bình phương của hướng được đề xuất là
\[
\|\mathbf{w}_j\|_{\mathcal{H}}^2
= \frac{1}{\lambda_j}\mathbf{v}_j^\top\widetilde{\mathbf{K}}\mathbf{v}_j = 1.
\]
Lấy tích vô hướng với $\widetilde{\Phi}(x)$ cho công thức tọa độ. Riêng với $x = x_i$, ta có $\widetilde{\mathbf{k}}_{x_i}^\top\mathbf{v}_j = (\widetilde{\mathbf{K}}\mathbf{v}_j)_i = \lambda_j v_{ji}$; do đó $y_j(x_i) = \sqrt{\lambda_j}\,v_{ji}$. Ghép $k$ hàng tọa độ cho toàn bộ mẫu thu được công thức ma trận.
\end{proof}

\paragraph{PCA là trường hợp riêng của KPCA.}
Khi kernel tuyến tính trên dữ liệu đã căn giữa, $\mathbf{K} = \mathbf{X}^\top\mathbf{X}$. Từ SVD $\mathbf{X} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{V}^\top$ suy ra
\begin{equation}
\mathbf{C} = \frac{1}{m}\mathbf{U}\boldsymbol{\Lambda}\mathbf{U}^\top,
\qquad
\mathbf{K} = \mathbf{V}\boldsymbol{\Lambda}\mathbf{V}^\top,
\qquad
\boldsymbol{\Lambda} = \boldsymbol{\Sigma}^2.
\label{eq:svd-relation}
\end{equation}
Nếu $\mathbf{K}\mathbf{v} = \lambda\mathbf{v}$ với $\lambda > 0$, thì $\mathbf{u} = \mathbf{X}\mathbf{v}/\sqrt{\lambda}$ là vector đơn vị và là vector riêng của $\mathbf{C}$ ứng với trị riêng $\lambda/m$. Chiếu một chiều lên $\mathbf{u}$ cho
\begin{equation}
\Phi(x)^\top \mathbf{u}
= \frac{\mathbf{k}_x^\top \mathbf{v}}{\sqrt{\lambda}}.
\label{eq:kpca-projection}
\end{equation}
Nếu $x = x_i$ trong mẫu thì tọa độ bằng $\sqrt{\lambda}\,v_i$. Toàn bộ KPCA chỉ cần eigendecomposition của $\mathbf{K}$---PCA là trường hợp $\mathbf{K} = \mathbf{X}^\top\mathbf{X}$.

\begin{example}[Kernel cho cấu trúc phi tuyến]
Hai vòng tròn đồng tâm trong mặt phẳng không thể tách theo bán kính bởi một phép chiếu tuyến tính một chiều: mọi trục đều trộn lẫn các điểm của hai vòng. Với kernel Gaussian $K(x,x') = \exp(-\gamma\|x-x'\|^2)$, các điểm trên cùng vòng có độ tương tự khác với các điểm ở vòng còn lại. KPCA có thể biểu diễn quan hệ phi tuyến này trong vài thành phần đầu; tham số $\gamma$ quyết định mức độ ``cục bộ'' của sự tương tự.
\end{example}

\subsection{Học manifold qua KPCA}
\label{sec:manifold}

Giả định dữ liệu cao chiều nằm gần một manifold nhúng. Thay vì chọn kernel ``có sẵn'', ta \emph{thiết kế} ma trận tương tự kernel từ đồ thị láng giềng hoặc khoảng cách địa hình, rồi áp dụng cùng khung KPCA (trích vector riêng hàng đầu hoặc nhỏ nhất tùy bài toán).

\subsubsection{Isomap}

Ý tưởng: giữ khoảng cách ``đi bộ'' trên manifold, xấp xỉ bằng đường đi ngắn nhất trên đồ thị $t$-láng giềng \cite{tenenbaum2000isomap,mohri2018foundations}.

\begin{enumerate}
  \item Dựng đồ thị vô hướng: nối mỗi điểm với $t$ láng giềng gần nhất (khoảng cách Euclidean).
  \item Trong hiện thực, nếu đồ thị bị rời thành nhiều thành phần, nối dần hai thành phần gần nhất bằng cạnh Euclidean ngắn nhất để có thể tính khoảng cách hữu hạn giữa mọi cặp điểm.
  \item Tính khoảng cách ngắn nhất $\delta_{ij}$ giữa mọi cặp đỉnh (ví dụ Floyd--Warshall).
  \item Từ ma trận khoảng cách bình phương $\boldsymbol{\Delta}$, đặt $\mathbf{K}_{\mathrm{Iso}} = -\frac{1}{2}\mathbf{H}\boldsymbol{\Delta}\mathbf{H}$.
  \item Lấy $k$ chiều từ $k$ trị riêng dương lớn nhất của $\mathbf{K}_{\mathrm{Iso}}$:
  \begin{equation}
  \mathbf{Y} = \boldsymbol{\Sigma}_{\mathrm{Iso},k}^{1/2}\,\mathbf{U}_{\mathrm{Iso},k}^\top.
  \label{eq:isomap-solution}
  \end{equation}
\end{enumerate}

\begin{lemma}[Double centering]
\label{lem:double-centering}
Cho $\mathbf{Z}=[\mathbf{z}_1,\ldots,\mathbf{z}_m]$ là ma trận điểm đã căn giữa, tức $\mathbf{Z}\mathbf{1}=\mathbf{0}$. Đặt $\Delta_{ij}=\|\mathbf{z}_i-\mathbf{z}_j\|_2^2$ và
\[
\mathbf{H}=\mathbf{I}_m-\frac{1}{m}\mathbf{1}\mathbf{1}^\top.
\]
Khi đó
\[
-\frac{1}{2}\mathbf{H}\boldsymbol{\Delta}\mathbf{H}=\mathbf{Z}^\top\mathbf{Z}.
\]
\end{lemma}

\begin{proof}
Đặt $q_i=\|\mathbf{z}_i\|_2^2$ và $\mathbf{q}=(q_1,\ldots,q_m)^\top$. Từ khai triển bình phương khoảng cách,
\[
\Delta_{ij}=\|\mathbf{z}_i-\mathbf{z}_j\|_2^2=q_i+q_j-2\mathbf{z}_i^\top\mathbf{z}_j.
\]
Dạng ma trận là
\[
\boldsymbol{\Delta}=\mathbf{q}\mathbf{1}^\top+\mathbf{1}\mathbf{q}^\top-2\mathbf{Z}^\top\mathbf{Z}.
\]
Vì $\mathbf{H}\mathbf{1}=\mathbf{0}$ nên hai hạng $\mathbf{q}\mathbf{1}^\top$ và $\mathbf{1}\mathbf{q}^\top$ biến mất khi nhân $\mathbf{H}$ hai phía. Ngoài ra, do $\mathbf{Z}\mathbf{1}=\mathbf{0}$ nên $\mathbf{Z}\mathbf{H}=\mathbf{Z}$. Suy ra
\[
-\frac{1}{2}\mathbf{H}\boldsymbol{\Delta}\mathbf{H}=\mathbf{H}\mathbf{Z}^\top\mathbf{Z}\mathbf{H}=\mathbf{Z}^\top\mathbf{Z}.
\]
\end{proof}

Bổ đề~\ref{lem:double-centering} giải thích vì sao Isomap xem $\mathbf{K}_{\mathrm{Iso}}=-\frac{1}{2}\mathbf{H}\boldsymbol{\Delta}\mathbf{H}$ như một ma trận Gram: nếu các $\delta_{ij}$ xấp xỉ khoảng cách Euclidean trong hệ tọa độ đã ``trải phẳng'', phép biến đổi sẽ khôi phục Gram của hệ tọa độ đó. Trong Isomap, $\Delta_{ij}$ không lấy từ khoảng cách Euclidean trực tiếp mà từ bình phương khoảng cách đường đi ngắn nhất trên đồ thị láng giềng.

\begin{lemma}[Embedding spectral của Isomap]
\label{lem:isomap-spectral}
Nếu $\mathbf{K}_{\mathrm{Iso}}$ xác định dương bán và có phân rã $\mathbf{K}_{\mathrm{Iso}} = \mathbf{U}\boldsymbol{\Sigma}\mathbf{U}^\top$ với các trị riêng giảm dần, thì ma trận tọa độ
\[
\mathbf{Y} = \boldsymbol{\Sigma}_{\mathrm{Iso},k}^{1/2}\,\mathbf{U}_{\mathrm{Iso},k}^\top
\]
thỏa $\mathbf{Y}^\top\mathbf{Y} = \mathbf{U}_{\mathrm{Iso},k}\boldsymbol{\Sigma}_{\mathrm{Iso},k}\mathbf{U}_{\mathrm{Iso},k}^\top$, là xấp xỉ Gram hạng $k$ tốt nhất của $\mathbf{K}_{\mathrm{Iso}}$ theo chuẩn Frobenius.
\end{lemma}

\begin{proof}
Theo định lý xấp xỉ hạng thấp Eckart--Young, cắt phân rã phổ tại $k$ trị riêng lớn nhất cho xấp xỉ hạng $k$ tối ưu của ma trận đối xứng xác định dương bán. Ma trận $\mathbf{Y}$ ở trên có Gram đúng bằng xấp xỉ bị cắt đó. Kết hợp với Bổ đề~\ref{lem:double-centering}, Gram gần đúng tương ứng với việc giữ gần các khoảng cách bình phương đã cung cấp cho bước MDS.
\end{proof}

Trên mẫu hữu hạn, $\mathbf{K}_{\mathrm{Iso}}$ đôi khi không xác định dương bán vì khoảng cách đồ thị không nhất thiết là khoảng cách Euclidean của một không gian phẳng. Isomap gần với scaling đa chiều cổ điển (MDS) nhưng dùng geodesic thay vì khoảng cách Euclidean trực tiếp. Số láng giềng $t$ cần chọn thận trọng: $t$ quá nhỏ có thể làm đồ thị đứt rời, buộc bước nối thành phần trong hiện thực thêm cạnh ngoài đồ thị láng giềng ban đầu; $t$ quá lớn lại tạo các cạnh tắt xuyên qua manifold.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  scale=0.95,
  vertex/.style={circle, draw=blue!60, fill=blue!12, minimum size=7mm, font=\small},
  edge/.style={thick, blue!70},
  geodesic/.style={thick, green!50!black},
  shortcut/.style={dashed, gray, thick}
]
  \node[vertex] (p1) at (0,2) {1};
  \node[vertex] (p2) at (1.8,1) {2};
  \node[vertex] (p3) at (1.8,-1) {3};
  \node[vertex] (p4) at (0,-2) {4};
  \node[vertex] (p5) at (-1.8,-1) {5};
  \node[vertex] (p6) at (-1.8,1) {6};
  \draw[edge] (p1)--(p2)--(p3)--(p4)--(p5)--(p6)--(p1);
  \draw[geodesic] (p1)--(p2)--(p3)--(p4);
  \draw[shortcut] (p1)--(p4);
  \node[font=\small, blue!70] at (2.8,0.3) {cạnh láng giềng};
  \node[font=\small, green!50!black] at (-2.9,-0.2) {đường geodesic};
  \node[font=\small, gray] at (0.9,-2.9) {đường tắt Euclidean};
\end{tikzpicture}
\caption{Isomap: khoảng cách trên manifold được xấp xỉ bằng tổng độ dài cạnh trên đường đi ngắn nhất (nét liền xanh lá), không dùng trực tiếp đoạn thẳng Euclidean (nét đứt).}
\label{fig:isomap-graph}
\end{figure}

\begin{example}[Trải phẳng Swiss roll]
Trên Swiss roll, hai điểm nằm ở hai lớp cuộn kế nhau có thể gần theo khoảng cách Euclidean nhưng xa nếu đi dọc theo bề mặt. Đồ thị láng giềng loại bỏ phần lớn đường tắt này; khoảng cách đường đi ngắn nhất do đó bám theo mặt cuộn. Bước double centering và phân rã phổ sau đó tạo embedding hai chiều gần với tờ giấy trước khi bị cuộn.
\end{example}

\begin{extension}
Chứng minh double centering ở Bổ đề~\ref{lem:double-centering} cũng cho thấy Isomap là KPCA với kernel được suy ra từ khoảng cách geodesic. Điểm khác biệt cốt lõi so với KPCA dùng Gaussian kernel là kernel này phụ thuộc vào toàn bộ đồ thị láng giềng và đường đi ngắn nhất, không chỉ vào từng cặp khoảng cách Euclidean ban đầu.
\end{extension}

% ================================================================
% ================================================================
\subsubsection{Laplacian Eigenmaps}
% ================================================================
 
\paragraph{Ý tưởng.}
Laplacian Eigenmaps là phương pháp giảm chiều phi tuyến dựa trên giả định
rằng dữ liệu nằm trên một đa tạp (manifold) có chiều thấp hơn không gian
gốc. Khác với Isomap (bảo toàn khoảng cách toàn cục), Laplacian Eigenmaps
chỉ yêu cầu \emph{các điểm lân cận trong không gian gốc phải vẫn ở gần nhau
trong không gian nhúng}.
 
% ------------------------------------------------
\paragraph{Bước 1 — Xây dựng đồ thị lân cận.}
% ------------------------------------------------
Cho tập dữ liệu $\{\mathbf{x}_i\}_{i=1}^m \subset \mathbb{R}^N$.
Với mỗi $\mathbf{x}_i$, ta nối cạnh tới $k$ điểm gần nhất
(hoặc mọi điểm trong vùng $\epsilon$-lân cận).
Trọng số cạnh được gán theo hàm Gauss:
\begin{equation}
  W_{ij} =
  \begin{cases}
    \exp\!\left(-\dfrac{\|\mathbf{x}_i - \mathbf{x}_j\|^2}{\sigma^2}\right)
      & \text{nếu } j \in \mathcal{N}_i, \\[4pt]
    0 & \text{ngược lại,}
  \end{cases}
\end{equation}
trong đó $\sigma > 0$ là siêu tham số băng thông.
Ma trận $\mathbf{W}$ đối xứng ($W_{ij}=W_{ji}$) và thưa.
 
% ------------------------------------------------
\paragraph{Bước 2 — Graph Laplacian.}
% ------------------------------------------------
Định nghĩa ma trận bậc đường chéo $\mathbf{D}$ với
$D_{ii} = \sum_{j=1}^m W_{ij}$.
Graph Laplacian (không chuẩn hoá) là
\begin{equation}
  \mathbf{L} = \mathbf{D} - \mathbf{W}.
\end{equation}
 
\begin{lemma}[Tính nửa xác định dương của $\mathbf{L}$]
$\mathbf{L}$ nửa xác định dương, tức $\mathbf{z}^\top\mathbf{L}\mathbf{z}
\ge 0$ với mọi $\mathbf{z}\in\mathbb{R}^m$.
\end{lemma}
\begin{proof}
Khai triển trực tiếp:
\begin{align*}
  \mathbf{z}^\top\mathbf{L}\mathbf{z}
  &= \mathbf{z}^\top\mathbf{D}\mathbf{z} - \mathbf{z}^\top\mathbf{W}\mathbf{z}
   = \sum_i D_{ii}z_i^2 - \sum_{i,j} W_{ij}z_iz_j \\
  &= \sum_i\!\left(\sum_j W_{ij}\right)z_i^2 - \sum_{i,j}W_{ij}z_iz_j.
\end{align*}
Vì $\mathbf{W}$ đối xứng, ta có $\sum_{i,j}W_{ij}z_i^2
= \tfrac{1}{2}\sum_{i,j}W_{ij}(z_i^2+z_j^2)$, nên
\begin{equation*}
  \mathbf{z}^\top\mathbf{L}\mathbf{z}
  = \tfrac{1}{2}\sum_{i,j}W_{ij}(z_i^2 + z_j^2 - 2z_iz_j)
  = \tfrac{1}{2}\sum_{i,j}W_{ij}(z_i - z_j)^2 \;\ge\; 0,
\end{equation*}
vì $W_{ij}\ge 0$.
\end{proof}
 
% ------------------------------------------------
\paragraph{Bước 3 — Bài toán tối ưu.}
% ------------------------------------------------
Gọi $\mathbf{Y} \in \mathbb{R}^{m \times k}$ là ma trận nhúng.
Ta tối thiểu hoá hàm mục tiêu bảo toàn lân cận
\begin{equation}\label{eq:le_obj}
  \min_{\mathbf{Y}}\;
  \Phi(\mathbf{Y}) \;:=\; \sum_{i,j} W_{ij}\|\mathbf{y}_i - \mathbf{y}_j\|^2
  \quad \text{s.t.} \quad
  \mathbf{Y}^\top \mathbf{D}\, \mathbf{Y} = \mathbf{I}_k.
\end{equation}
Cặp điểm có $W_{ij}$ lớn (gần nhau trong không gian gốc) sẽ bị phạt nặng
nếu $\|\mathbf{y}_i - \mathbf{y}_j\|$ lớn.
Ràng buộc $\mathbf{Y}^\top\mathbf{D}\mathbf{Y}=\mathbf{I}_k$ loại bỏ nghiệm
tầm thường và chuẩn hoá theo bậc đỉnh.
 
% ------------------------------------------------
\paragraph{Chứng minh: dạng trace.}
% ------------------------------------------------
 
\begin{lemma}\label{lem:le_trace}
$\displaystyle\sum_{i,j}W_{ij}\|\mathbf{y}_i-\mathbf{y}_j\|^2
 = 2\operatorname{Tr}(\mathbf{Y}^\top\mathbf{L}\mathbf{Y})$.
\end{lemma}
\begin{proof}
Khai triển bình phương khoảng cách:
\begin{equation}\label{eq:le_expand}
  \sum_{i,j}W_{ij}\|\mathbf{y}_i - \mathbf{y}_j\|^2
  = \sum_{i,j}W_{ij}\bigl(\mathbf{y}_i^\top\mathbf{y}_i
    + \mathbf{y}_j^\top\mathbf{y}_j
    - 2\,\mathbf{y}_i^\top\mathbf{y}_j\bigr).
\end{equation}
Ta xử lý từng số hạng. Vì $D_{ii}=\sum_j W_{ij}$:
\begin{equation*}
  \sum_{i,j}W_{ij}\,\mathbf{y}_i^\top\mathbf{y}_i
  = \sum_i D_{ii}\,\mathbf{y}_i^\top\mathbf{y}_i
  = \operatorname{Tr}(\mathbf{Y}^\top\mathbf{D}\mathbf{Y}).
\end{equation*}
Tương tự (đổi vai trò $i\leftrightarrow j$, dùng $\mathbf{W}$ đối xứng):
\begin{equation*}
  \sum_{i,j}W_{ij}\,\mathbf{y}_j^\top\mathbf{y}_j
  = \sum_j D_{jj}\,\mathbf{y}_j^\top\mathbf{y}_j
  = \operatorname{Tr}(\mathbf{Y}^\top\mathbf{D}\mathbf{Y}).
\end{equation*}
Số hạng chéo:
\begin{equation*}
  \sum_{i,j}W_{ij}\,\mathbf{y}_i^\top\mathbf{y}_j
  = \operatorname{Tr}(\mathbf{Y}^\top\mathbf{W}\mathbf{Y}).
\end{equation*}
Thay vào~\eqref{eq:le_expand}:
\begin{equation*}
  \sum_{i,j}W_{ij}\|\mathbf{y}_i-\mathbf{y}_j\|^2
  = 2\operatorname{Tr}(\mathbf{Y}^\top\mathbf{D}\mathbf{Y})
    - 2\operatorname{Tr}(\mathbf{Y}^\top\mathbf{W}\mathbf{Y})
  = 2\operatorname{Tr}\!\bigl(\mathbf{Y}^\top(\mathbf{D}-\mathbf{W})\mathbf{Y}\bigr)
  = 2\operatorname{Tr}(\mathbf{Y}^\top\mathbf{L}\mathbf{Y}). \qedhere
\end{equation*}
\end{proof}
 
Bỏ hằng số $2$, bài toán~\eqref{eq:le_obj} tương đương với
\begin{equation}\label{eq:le_trace}
  \min_{\mathbf{Y}}\;\operatorname{Tr}(\mathbf{Y}^\top\mathbf{L}\mathbf{Y})
  \quad\text{s.t.}\quad
  \mathbf{Y}^\top\mathbf{D}\mathbf{Y}=\mathbf{I}_k.
\end{equation}
 
% ------------------------------------------------
\paragraph{Chứng minh: nghiệm tối ưu bằng nhân tử Lagrange.}
% ------------------------------------------------
 
\begin{theorem}\label{thm:le_solution}
Nghiệm tối ưu của bài toán~\eqref{eq:le_trace} là các cột của $\mathbf{Y}$
gồm $k$ vector riêng suy rộng ứng với $k$ trị riêng nhỏ nhất (khác $0$) của
\begin{equation}\label{eq:le_geig}
  \mathbf{L}\,\mathbf{y} = \lambda\,\mathbf{D}\,\mathbf{y}.
\end{equation}
\end{theorem}
\begin{proof}
Viết hàm Lagrange với nhân tử ma trận $\boldsymbol{\Lambda}\in\mathbb{R}^{k\times k}$
(đối xứng vì ràng buộc đối xứng):
\begin{equation*}
  \mathcal{L}(\mathbf{Y},\boldsymbol{\Lambda})
  = \operatorname{Tr}(\mathbf{Y}^\top\mathbf{L}\mathbf{Y})
  - \operatorname{Tr}\!\bigl[\boldsymbol{\Lambda}
    \bigl(\mathbf{Y}^\top\mathbf{D}\mathbf{Y}-\mathbf{I}_k\bigr)\bigr].
\end{equation*}
Lấy đạo hàm theo $\mathbf{Y}$ (dùng công thức $\frac{\partial}{\partial\mathbf{Y}}
\operatorname{Tr}(\mathbf{Y}^\top\mathbf{A}\mathbf{Y})=(\mathbf{A}+\mathbf{A}^\top)\mathbf{Y}$;
vì $\mathbf{L}$ và $\mathbf{D}$ đối xứng, ta được hệ số $2$):
\begin{equation*}
  \frac{\partial\mathcal{L}}{\partial\mathbf{Y}}
  = 2\mathbf{L}\mathbf{Y} - 2\mathbf{D}\mathbf{Y}\boldsymbol{\Lambda} = \mathbf{0}.
\end{equation*}
Suy ra điều kiện stationarity:
\begin{equation}\label{eq:le_stat}
  \mathbf{L}\mathbf{Y} = \mathbf{D}\mathbf{Y}\boldsymbol{\Lambda}.
\end{equation}
Nhân trái bởi $\mathbf{Y}^\top$ và dùng ràng buộc
$\mathbf{Y}^\top\mathbf{D}\mathbf{Y}=\mathbf{I}_k$:
\begin{equation*}
  \mathbf{Y}^\top\mathbf{L}\mathbf{Y}
  = \mathbf{Y}^\top\mathbf{D}\mathbf{Y}\boldsymbol{\Lambda}
  = \boldsymbol{\Lambda}.
\end{equation*}
Vậy $\boldsymbol{\Lambda} = \mathbf{Y}^\top\mathbf{L}\mathbf{Y}$ và giá trị
hàm mục tiêu tại nghiệm stationarity bằng
$\operatorname{Tr}(\boldsymbol{\Lambda})$.
Để tối thiểu hoá $\operatorname{Tr}(\boldsymbol{\Lambda})$, ta phân tích
phương trình~\eqref{eq:le_stat} theo cột: cột thứ $\ell$ của $\mathbf{Y}$
là $\mathbf{y}_\ell$ thoả
\begin{equation*}
  \mathbf{L}\,\mathbf{y}_\ell = \lambda_\ell\,\mathbf{D}\,\mathbf{y}_\ell,
\end{equation*}
tức $\mathbf{y}_\ell$ là vector riêng suy rộng~\eqref{eq:le_geig} với trị
riêng $\lambda_\ell$, và
$\operatorname{Tr}(\boldsymbol{\Lambda})=\sum_{\ell=1}^k\lambda_\ell$.
 
Để tối thiểu hoá tổng này, ta chọn $k$ trị riêng nhỏ nhất. Trị riêng nhỏ
nhất $\lambda_1=0$ ứng với vector riêng $\mathbf{y}_1\propto\mathbf{1}$ (hằng
số), không phân biệt được các điểm, nên bị loại bỏ. Do đó nghiệm tối ưu
chứa các vector riêng $\mathbf{y}_2,\ldots,\mathbf{y}_{k+1}$ ứng với
$0 < \lambda_2 \le \cdots \le \lambda_{k+1}$.
\end{proof}
 
\begin{remark}
Nếu đồ thị có $c$ thành phần liên thông, trị riêng $0$ có bội số $c$; khi
đó ta loại bỏ đúng $c$ vector riêng hằng số và lấy $k$ vector riêng tiếp
theo.
\end{remark}
 
% ------------------------------------------------
\paragraph{Liên hệ với Kernel PCA.}
% ------------------------------------------------
Thông qua giả nghịch đảo Moore–Penrose, đặt $\mathbf{K}_L = \mathbf{L}^\dagger$.
Nhúng do Laplacian Eigenmaps (ứng với các trị riêng nhỏ nhất của $\mathbf{L}$)
tỉ lệ thuận với các thành phần chính (ứng với các trị riêng lớn nhất) của
Kernel PCA trên $\mathbf{K}_L$. Toàn bộ quá trình có thể xem là Kernel PCA
với kernel thiết kế cho cấu trúc đồ thị.
 
% ------------------------------------------------
\paragraph{Ưu điểm và Hạn chế.}\mbox{}\\[2pt]
% ------------------------------------------------
\textbf{Ưu điểm:}
Bảo toàn tốt cấu trúc phi tuyến cục bộ; tính toán nhanh vì $\mathbf{L}$ thưa.
 
\textbf{Hạn chế:}
Bỏ qua cấu trúc toàn cục; số láng giềng $k$ và băng thông $\sigma$ ảnh
hưởng mạnh đến kết quả và cần tinh chỉnh cẩn thận.
 
% ================================================================
\subsubsection{Locally Linear Embedding (LLE)}
% ================================================================
 
\paragraph{Ý tưởng.}
LLE không bảo toàn khoảng cách mà bảo toàn \emph{cấu trúc tuyến tính cục
bộ}: mỗi điểm $\mathbf{x}_i$ được xấp xỉ bởi một tổ hợp lồi của các láng
giềng $\mathcal{N}_i$, và tổ hợp đó phải được tái hiện trong không gian nhúng.
 
% ================================================
\paragraph{Bước 1 — Tìm trọng số tái tạo.}
% ================================================
Với mỗi điểm $\mathbf{x}_i$, giải bài toán
\begin{equation}\label{eq:lle_w}
  \min_{\{W_{ij}\}_{j\in\mathcal{N}_i}}\;
  \left\|\mathbf{x}_i - \sum_{j \in \mathcal{N}_i} W_{ij}\,\mathbf{x}_j\right\|^2
  \quad \text{s.t.} \quad
  \sum_{j \in \mathcal{N}_i} W_{ij} = 1.
\end{equation}
Ràng buộc đơn vị đảm bảo bất biến tịnh tiến: nếu $\mathbf{x}_i \mapsto
\mathbf{x}_i + \mathbf{t}$ với mọi $i$, các trọng số $W_{ij}$ không thay đổi.
 
\begin{theorem}[Nghiệm dạng đóng của bài toán~\eqref{eq:lle_w}]
\label{thm:lle_w}
Định nghĩa ma trận Gram cục bộ $\mathbf{C}^{(i)}\in\mathbb{R}^{|\mathcal{N}_i|
\times |\mathcal{N}_i|}$ với các phần tử
\begin{equation}
  C^{(i)}_{jk} = (\mathbf{x}_i - \mathbf{x}_j)^\top(\mathbf{x}_i - \mathbf{x}_k),
  \quad j,k\in\mathcal{N}_i.
\end{equation}
Nghiệm tối ưu của~\eqref{eq:lle_w} là
\begin{equation}\label{eq:lle_weights}
  W_{ij}
  = \frac{\displaystyle\sum_{k\in\mathcal{N}_i}\bigl(\mathbf{C}^{(i)}\bigr)^{-1}_{jk}}
         {\displaystyle\sum_{s,t\in\mathcal{N}_i}\bigl(\mathbf{C}^{(i)}\bigr)^{-1}_{st}}.
\end{equation}
\end{theorem}
\begin{proof}
\textit{Bước i — Đặt lại hàm mục tiêu.}
Từ ràng buộc $\sum_j W_{ij}=1$, ta viết
$\mathbf{x}_i = \sum_{j\in\mathcal{N}_i}W_{ij}\,\mathbf{x}_i$.
Khi đó:
\begin{equation*}
  \mathbf{x}_i - \sum_{j\in\mathcal{N}_i}W_{ij}\mathbf{x}_j
  = \sum_{j\in\mathcal{N}_i}W_{ij}(\mathbf{x}_i - \mathbf{x}_j).
\end{equation*}
Đặt $\mathbf{z}_{ij}=\mathbf{x}_i-\mathbf{x}_j$ và $\mathbf{w}_i=(W_{ij})_{j\in
\mathcal{N}_i}$, hàm mục tiêu trở thành
\begin{equation*}
  \varepsilon_i
  = \left\|\sum_{j\in\mathcal{N}_i}W_{ij}\,\mathbf{z}_{ij}\right\|^2
  = \mathbf{w}_i^\top\mathbf{C}^{(i)}\mathbf{w}_i.
\end{equation*}
 
\textit{Bước ii — Nhân tử Lagrange.}
Bài toán constrained trở thành:
\begin{equation*}
  \mathcal{L}(\mathbf{w}_i,\lambda)
  = \mathbf{w}_i^\top\mathbf{C}^{(i)}\mathbf{w}_i
  - \lambda\bigl(\mathbf{1}^\top\mathbf{w}_i - 1\bigr).
\end{equation*}
Lấy đạo hàm và cho bằng $0$:
\begin{equation}\label{eq:lle_deriv}
  \frac{\partial\mathcal{L}}{\partial\mathbf{w}_i}
  = 2\mathbf{C}^{(i)}\mathbf{w}_i - \lambda\mathbf{1} = \mathbf{0}
  \quad\Longrightarrow\quad
  \mathbf{w}_i = \frac{\lambda}{2}\bigl(\mathbf{C}^{(i)}\bigr)^{-1}\mathbf{1}.
\end{equation}
 
\textit{Bước iii — Chuẩn hoá.}
Áp ràng buộc $\mathbf{1}^\top\mathbf{w}_i=1$ vào~\eqref{eq:lle_deriv}:
\begin{equation*}
  1 = \frac{\lambda}{2}\,\mathbf{1}^\top\bigl(\mathbf{C}^{(i)}\bigr)^{-1}\mathbf{1}
  \quad\Longrightarrow\quad
  \frac{\lambda}{2}
  = \frac{1}{\mathbf{1}^\top(\mathbf{C}^{(i)})^{-1}\mathbf{1}}.
\end{equation*}
Thay lại vào~\eqref{eq:lle_deriv}:
\begin{equation*}
  \mathbf{w}_i
  = \frac{(\mathbf{C}^{(i)})^{-1}\mathbf{1}}
         {\mathbf{1}^\top(\mathbf{C}^{(i)})^{-1}\mathbf{1}},
\end{equation*}
tức là
$W_{ij}
 =\dfrac{\sum_{k}(\mathbf{C}^{(i)})^{-1}_{jk}}
        {\sum_{s,t}(\mathbf{C}^{(i)})^{-1}_{st}}$,
đúng như~\eqref{eq:lle_weights}.
\end{proof}
 
\begin{remark}
Nếu $|\mathcal{N}_i| > N$, ma trận $\mathbf{C}^{(i)}$ có thể suy biến; trong
thực tế ta thêm chính quy hoá $\mathbf{C}^{(i)} \leftarrow \mathbf{C}^{(i)}
+ \varepsilon\mathbf{I}$ với $\varepsilon$ nhỏ.
\end{remark}
 
% ================================================
\paragraph{Bước 2 — Nhúng bảo toàn cấu trúc.}
% ================================================
Với ma trận trọng số $\mathbf{W}$ cố định (mở rộng về $\mathbb{R}^{m\times m}$
bằng cách đặt $W_{ij}=0$ với $j\notin\mathcal{N}_i$), tìm nhúng
$\mathbf{Y}\in\mathbb{R}^{m\times k}$ bằng cách giải
\begin{equation}\label{eq:lle_embed}
  \min_{\mathbf{Y}}\;
  \sum_{i=1}^m
  \left\|\mathbf{y}_i - \sum_{j=1}^m W_{ij}\,\mathbf{y}_j\right\|^2
  \quad\text{s.t.}\quad
  \frac{1}{m}\mathbf{Y}^\top\mathbf{Y} = \mathbf{I}_k,\quad
  \mathbf{Y}^\top\mathbf{1} = \mathbf{0}.
\end{equation}
Ràng buộc đầu loại bỏ co giãn tuỳ ý; ràng buộc sau loại bỏ tịnh tiến
(vector riêng hằng số $\propto\mathbf{1}$).
 
\begin{theorem}[Nghiệm tối ưu của bài toán~\eqref{eq:lle_embed}]
\label{thm:lle_embed}
Đặt $\mathbf{M}=(\mathbf{I}-\mathbf{W})^\top(\mathbf{I}-\mathbf{W})$.
Nghiệm tối ưu của~\eqref{eq:lle_embed} là $k$ vector riêng của $\mathbf{M}$
ứng với $k$ trị riêng nhỏ nhất khác $0$.
\end{theorem}
\begin{proof}
\textit{Bước i — Viết lại hàm mục tiêu dạng trace.}
Ký hiệu $\mathbf{R}=(\mathbf{I}-\mathbf{W})\mathbf{Y}\in\mathbb{R}^{m\times k}$,
trong đó dòng thứ $i$ của $\mathbf{R}$ là
$\mathbf{r}_i^\top = \mathbf{y}_i^\top - \sum_j W_{ij}\mathbf{y}_j^\top$.
Khi đó:
\begin{align*}
  \sum_{i=1}^m\left\|\mathbf{y}_i - \sum_j W_{ij}\mathbf{y}_j\right\|^2
  &= \sum_{i=1}^m \|\mathbf{r}_i\|^2
   = \|\mathbf{R}\|_F^2
   = \operatorname{Tr}(\mathbf{R}^\top\mathbf{R}) \\
  &= \operatorname{Tr}\!\bigl[
       \bigl((\mathbf{I}-\mathbf{W})\mathbf{Y}\bigr)^\top
       \bigl((\mathbf{I}-\mathbf{W})\mathbf{Y}\bigr)
     \bigr]
   = \operatorname{Tr}(\mathbf{Y}^\top\mathbf{M}\mathbf{Y}),
\end{align*}
với $\mathbf{M}=(\mathbf{I}-\mathbf{W})^\top(\mathbf{I}-\mathbf{W})$.
 
\textit{Bước ii — Tính chất của $\mathbf{M}$.}
$\mathbf{M}$ nửa xác định dương vì
$\mathbf{z}^\top\mathbf{M}\mathbf{z}
=\|(\mathbf{I}-\mathbf{W})\mathbf{z}\|^2\ge 0$.
Vector $\mathbf{1}$ là vector riêng của $\mathbf{M}$ ứng với
$\lambda_1=0$, vì $\sum_j W_{ij}=1$ dẫn đến
$(\mathbf{I}-\mathbf{W})\mathbf{1}=\mathbf{0}$.
Ràng buộc $\mathbf{Y}^\top\mathbf{1}=\mathbf{0}$
loại bỏ thành phần này khỏi không gian tìm kiếm.
 
\textit{Bước iii — Nhân tử Lagrange.}
Với ràng buộc $\frac{1}{m}\mathbf{Y}^\top\mathbf{Y}=\mathbf{I}_k$
(tương đương $\mathbf{Y}^\top\mathbf{Y}=m\mathbf{I}_k$), viết hàm Lagrange
với nhân tử ma trận đối xứng $\boldsymbol{\Lambda}$:
\begin{equation*}
  \mathcal{L}(\mathbf{Y},\boldsymbol{\Lambda})
  = \operatorname{Tr}(\mathbf{Y}^\top\mathbf{M}\mathbf{Y})
  - \operatorname{Tr}\!\bigl[
      \boldsymbol{\Lambda}(\mathbf{Y}^\top\mathbf{Y} - m\mathbf{I}_k)
    \bigr].
\end{equation*}
Đạo hàm theo $\mathbf{Y}$ và cho bằng $0$:
\begin{equation*}
  \frac{\partial\mathcal{L}}{\partial\mathbf{Y}}
  = 2\mathbf{M}\mathbf{Y} - 2\mathbf{Y}\boldsymbol{\Lambda} = \mathbf{0}
  \quad\Longrightarrow\quad
  \mathbf{M}\mathbf{Y} = \mathbf{Y}\boldsymbol{\Lambda}.
\end{equation*}
 
\textit{Bước iv — Diagonalization.}
Theo cột: $\mathbf{M}\mathbf{y}_\ell = \lambda_\ell\mathbf{y}_\ell$,
tức mỗi cột $\mathbf{y}_\ell$ là vector riêng của $\mathbf{M}$.
Nhân trái $\mathbf{Y}^\top$:
\begin{equation*}
  \mathbf{Y}^\top\mathbf{M}\mathbf{Y}
  = \mathbf{Y}^\top\mathbf{Y}\boldsymbol{\Lambda}
  = m\boldsymbol{\Lambda},
\end{equation*}
nên $\operatorname{Tr}(\mathbf{Y}^\top\mathbf{M}\mathbf{Y})
=m\operatorname{Tr}(\boldsymbol{\Lambda})=m\sum_{\ell=1}^k\lambda_\ell$.
Để tối thiểu hoá, chọn $k$ trị riêng nhỏ nhất của $\mathbf{M}$.
Loại bỏ $\lambda_1=0$ (đã bị khử bởi ràng buộc $\mathbf{Y}^\top\mathbf{1}=\mathbf{0}$),
nghiệm là các vector riêng $\mathbf{y}_2,\ldots,\mathbf{y}_{k+1}$ ứng với
$0<\lambda_2\le\cdots\le\lambda_{k+1}$.
\end{proof}
 
% ------------------------------------------------
\paragraph{Liên hệ với Kernel PCA.}
% ------------------------------------------------
Coi $\mathbf{K}_{\mathrm{LLE}}=\mathbf{M}^\dagger$ như một kernel,
nhúng LLE tương đương Kernel PCA trên kernel cục bộ này: các thành phần chính
lớn nhất của KPCA khớp với các vector riêng nhỏ nhất của $\mathbf{M}$.
 
% ------------------------------------------------
\paragraph{Ưu điểm và Hạn chế.}\mbox{}\\[2pt]
% ------------------------------------------------
\textbf{Ưu điểm:}
Bảo toàn tốt quan hệ tuyến tính cục bộ; hiệu quả với đa tạp phi tuyến mà
PCA tuyến tính không xử lý được.
 
\textbf{Hạn chế:}
Nhạy với số láng giềng $k$; không bảo toàn cấu trúc toàn cục; nhạy với
nhiễu; chi phí $O(m^2)$ cho bước giải trị riêng với mẫu lớn.
\subsection{Trực quan hóa phi tuyến: t-SNE và UMAP}
\label{sec:extension-viz}

\begin{extension}
Phần sau không có trong \cite{mohri2018foundations}. Các phương pháp ở Mục~\ref{sec:manifold} tìm $\mathbf{Y}$ bằng phân rã phổ trên đồ thị (tối ưu tuyến tính). Ở đây ta tối ưu trực tiếp từng tọa độ nhúng $\mathbf{y}_i \in \R^d$ với $d \ll D$ (thường $d=2$) để vẽ trực quan \cite{vandermaaten2008tsne,mcinnes2018umap}.
\end{extension}

\subsubsection{t-Distributed Stochastic Neighbor Embedding (t-SNE)}
\label{sec:tsne}

Cho $\mathcal{X} = \{\mathbf{x}_i\}_{i=1}^N \subset \R^D$. t-SNE \cite{vandermaaten2008tsne} so khớp hai phân phối láng giềng: một trên $\mathcal{X}$, một trên $\{\mathbf{y}_i\}$.

\paragraph{Bước 1 --- Láng giềng trong không gian gốc.}
Với mỗi $i$, xác suất ``$j$ là láng giềng của $i$'' dùng nhân tử Gaussian; $\sigma_i$ chọn sao cho \emph{perplexity} (mức láng giềng hiệu dụng) cố định:
\[
p_{j \mid i} = \frac{\exp\!\left(-\frac{\|\mathbf{x}_i - \mathbf{x}_j\|^2}{2\sigma_i^2}\right)}
{\sum_{k \neq i} \exp\!\left(-\frac{\|\mathbf{x}_i - \mathbf{x}_k\|^2}{2\sigma_i^2}\right)}.
\]
Về mặt thông tin, nếu $P_i$ là phân phối có các xác suất $p_{j|i}$ thì
\[
  \mathrm{Perp}(P_i)=2^{H(P_i)}, \qquad
  H(P_i)=-\sum_{j\ne i} p_{j|i}\log_2 p_{j|i}.
\]
Perplexity có thể hiểu như số láng giềng hiệu dụng của điểm $i$. Trong thực
hành, $\sigma_i$ được tìm bằng tìm kiếm nhị phân sao cho entropy của $P_i$
phù hợp với perplexity đã chọn: perplexity nhỏ nhấn mạnh cấu trúc rất cục bộ,
còn perplexity lớn nhìn nhiều láng giềng hơn. Ma trận đối xứng
$p_{ij} = \frac{p_{j|i}+p_{i|j}}{2N}$ dùng khi tính phân kỳ.

\paragraph{Bước 2 --- Láng giềng trong không gian nhúng.}
Dùng Student-$t$ (đuôi nặng) thay Gaussian để giảm \emph{crowding} (điểm chen chúc khi $d$ nhỏ):
\[
q_{ij} = \frac{\left(1 + \|\mathbf{y}_i - \mathbf{y}_j\|^2\right)^{-1}}
{\sum_{k \neq \ell} \left(1 + \|\mathbf{y}_k - \mathbf{y}_\ell\|^2\right)^{-1}}.
\]
Khi giảm từ không gian cao chiều xuống hai chiều, không gian nhúng thường không
đủ ``diện tích'' để giữ tất cả láng giềng gần nhau như ban đầu. Đuôi nặng của
Student-$t$ giúp các điểm tương đối xa trong không gian nhúng vẫn tạo lực đẩy
đáng kể, nhờ đó các cụm ít bị nén chồng lên nhau.

\paragraph{Bước 3 --- Tối ưu.}
Tìm $\{\mathbf{y}_i\}$ sao cho $q_{ij}$ gần $p_{ij}$ theo phân kỳ KL:
\[
\min_{\{\mathbf{y}_i\}} \mathrm{KL}(P \| Q) = \sum_{i \neq j} p_{ij} \log \frac{p_{ij}}{q_{ij}}.
\]
Phân kỳ KL ở đây là bất đối xứng: nếu $p_{ij}$ lớn nhưng $q_{ij}$ nhỏ, tức là
hai điểm gần trong không gian gốc bị đặt xa trong nhúng, chi phí tăng mạnh.
Ngược lại, hai điểm xa trong không gian gốc bị đặt gần nhau thường bị phạt yếu
hơn. Vì vậy t-SNE đặc biệt phù hợp để bảo toàn cấu trúc cục bộ và trực quan hóa
cụm, nhưng khoảng cách giữa các cụm hoặc kích thước cụm trong hình 2D không nên
được diễn giải như khoảng cách toàn cục thật.

Gradient của hàm mục tiêu có dạng lực hút--đẩy:
\[
  \frac{\partial \mathrm{KL}(P\|Q)}{\partial \mathbf{y}_i}
  =
  4 \sum_j (p_{ij}-q_{ij})
  (\mathbf{y}_i-\mathbf{y}_j)
  \left(1+\|\mathbf{y}_i-\mathbf{y}_j\|^2\right)^{-1}.
\]
Khi $p_{ij} > q_{ij}$, cặp điểm bị kéo lại gần nhau; khi $p_{ij} < q_{ij}$,
cặp điểm bị đẩy ra xa. Cập nhật thường dùng gradient descent với momentum,
early exaggeration ở giai đoạn đầu và learning rate thích hợp. Barnes--Hut
xấp xỉ lực với độ phức tạp $O(N \log N)$; các cài đặt hiện đại còn có thể dùng
FFT/interpolation để tăng tốc.

\begin{algorithm}[H]
\caption{t-SNE (tóm tắt)}
\label{alg:tsne}
\begin{algorithmic}[1]
\Require Tập $\mathbf{X}$, số chiều nhúng $d$, perplexity, số vòng lặp
\State Tính ma trận $p_{ij}$ (Gaussian có $\sigma_i$ theo perplexity)
\State Khởi tạo ngẫu nhiên $\mathbf{y}_i \in \R^d$
\For{mỗi vòng lặp}
  \State Tính $q_{ij}$ từ khoảng cách Student-$t$ giữa các $\mathbf{y}_i$
  \State Cập nhật $\mathbf{y}_i \leftarrow \mathbf{y}_i - \eta \, \frac{\partial \mathrm{KL}}{\partial \mathbf{y}_i}$
\EndFor
\State \Return $\{\mathbf{y}_i\}$
\end{algorithmic}
\end{algorithm}

Trong mã nguồn của đồ án, t-SNE được dùng như một baseline mở rộng thông qua
wrapper \texttt{TSNE} quanh \texttt{sklearn.manifold.TSNE}, không phải phần
cài đặt from-scratch. Các tham số chính được truyền từ notebook là
\texttt{perplexity}, \texttt{max\_iter}, \texttt{init} và
\texttt{learning\_rate}; kết quả thực nghiệm tương ứng được báo cáo ở
Mục~\ref{sec:experiments}.

\subsubsection{Uniform Manifold Approximation and Projection (UMAP)}
\label{sec:umap}

\paragraph{Ý tưởng chính.}
UMAP \cite{mcinnes2018umap} giải quyết bài toán: cho tập dữ liệu cao chiều ($D$ chiều), tìm cách biểu diễn chúng trong không gian thấp chiều ($d = 2$ hoặc $3$) để trực quan hóa, sao cho \emph{những điểm gần nhau trong không gian gốc vẫn gần nhau, và những điểm xa nhau vẫn xa nhau} trong không gian mới.

Cách tiếp cận: (1) mô hình hóa quan hệ láng giềng bằng một \emph{đồ thị có trọng số mờ} trong không gian gốc, (2) tìm tọa độ mới sao cho đồ thị mờ tương ứng trong không gian thấp giống đồ thị gốc nhất có thể.

\paragraph{Ý nghĩa.}
\begin{itemize}
  \item Giữ được cả cấu trúc \emph{cục bộ} (láng giềng gần) lẫn \emph{toàn cục} (quan hệ giữa các cụm) tốt hơn t-SNE.
  \item Nhanh hơn t-SNE trên dữ liệu lớn nhờ tối ưu SGD trên đồ thị thưa.
  \item Dùng để trực quan hóa dữ liệu cao chiều (giảm về 2D/3D).
\end{itemize}

\paragraph{Ba giả định.}
UMAP dựa trên giả định manifold (Định nghĩa~\ref{def:manifold}) và cấu trúc tô-pô (Định nghĩa~\ref{def:topology}) đã trình bày ở Chương~2.
UMAP giả định mẫu $\mathcal{X} = \{\mathbf{x}_i\}_{i=1}^N \subset \R^D$ nằm trên một đa tạp $(M,g)$ chưa biết, với:
\begin{enumerate}
  \item \textbf{Phân bố đều:} dữ liệu trải đều trên $M$ (vùng nào cũng có mật độ tương đương).
  \item \textbf{Metric gần hằng cục bộ:} trong vùng nhỏ, khoảng cách trên manifold $\approx$ khoảng cách Euclid (sau chuẩn hóa tỉ lệ).
  \item \textbf{Liên thông cục bộ:} từ mỗi điểm luôn tồn tại đường đi trên $M$ tới các láng giềng.
\end{enumerate}
Khi dữ liệu thực tế không đều, UMAP tự điều chỉnh thước đo cục bộ ($\rho_i$, $\sigma_i$) quanh từng điểm để ``cân bằng'' vùng thưa và vùng dày.

\paragraph{Các bước thực hiện.}

\textbf{Bước 1 --- Xây đồ thị mờ cục bộ} (``ai là láng giềng của ai, ở mức nào?'')

Với mỗi điểm $\mathbf{x}_i$, tìm $n$ láng giềng gần nhất $\mathcal{N}_i$. Gọi $d_{ij} = \|\mathbf{x}_i - \mathbf{x}_j\|$. Tính:
\begin{align}
\rho_i &= \min\{ d_{ij} : d_{ij} > 0,\; j \in \mathcal{N}_i \}, \\
v_{j \mid i} &= \exp\!\left(\frac{-\max(0,\, d_{ij} - \rho_i)}{\sigma_i}\right),
\end{align}
trong đó $\sigma_i$ được chọn bằng tìm kiếm nhị phân sao cho $\sum_{j \in \mathcal{N}_i} v_{j|i} = \log_2 n$.

Giải thích trực quan:
\begin{itemize}
  \item $\rho_i$ = khoảng cách tới láng giềng gần nhất. Trừ đi $\rho_i$ để đảm bảo luôn có ít nhất một cạnh với trọng số $= 1$ (liên thông cục bộ).
  \item $\sigma_i$ = ``bán kính nhìn'' của $\mathbf{x}_i$. Vùng thưa $\to$ $\sigma_i$ lớn (nhìn xa hơn); vùng dày $\to$ $\sigma_i$ nhỏ.
  \item $v_{j|i} \in [0,1]$ = mức tin cậy rằng ``$j$ là láng giềng thực sự của $i$''. Gần $\to$ gần $1$; xa $\to$ gần $0$.
\end{itemize}

\textbf{Bước 2 --- Hợp thành đồ thị toàn cục} (đối xứng hóa)

Vì $v_{j|i}$ và $v_{i|j}$ thường khác nhau (mỗi điểm có ``góc nhìn'' riêng), UMAP đối xứng hóa bằng phép hợp xác suất:
\begin{equation}
B_{ij} = v_{j \mid i} + v_{i \mid j} - v_{j \mid i}\, v_{i \mid j}.
\label{eq:umap-fuzzy-union}
\end{equation}
Ý nghĩa: $B_{ij}$ cao khi \emph{ít nhất một trong hai} điểm coi điểm kia là láng giềng mạnh. Ma trận $\mathbf{B}$ mô tả toàn bộ cấu trúc tô-pô của dữ liệu gốc.

\textbf{Bước 3 --- Khởi tạo spectral} (vị trí ban đầu trong 2D)

Dùng Laplacian chuẩn hóa $\mathbf{L} = \mathbf{D}^{-\frac{1}{2}}(\mathbf{D}-\mathbf{B})\mathbf{D}^{-\frac{1}{2}}$ (với $D_{ii} = \sum_j B_{ij}$); lấy $d$ vector riêng ứng $d$ trị riêng nhỏ nhất (bỏ trivial) làm tọa độ ban đầu $\mathbf{Y}$. Bước này cho layout ban đầu đã phản ánh sơ bộ cấu trúc toàn cục, giúp SGD hội tụ nhanh hơn.

\textbf{Bước 4 --- Tối ưu nhúng bằng SGD} (``kéo gần, đẩy xa'')

Trong không gian nhúng, độ tương đồng giữa $\mathbf{y}_i$ và $\mathbf{y}_j$ được định nghĩa:
\[
w_{ij} = \frac{1}{1 + a\, d_{ij}^{2b}}, \quad d_{ij} = \|\mathbf{y}_i - \mathbf{y}_j\|,
\]
với $a, b > 0$ được chọn từ tham số \texttt{min\_dist}. Hàm $w_{ij}$ có dạng Student-$t$: gần $\to$ $w_{ij} \approx 1$; xa $\to$ $w_{ij} \approx 0$.

UMAP tối thiểu \emph{fuzzy cross-entropy} giữa hai tập mờ cạnh: $B_{ij}$ (không gian gốc) và $w_{ij}$ (không gian nhúng):
\begin{equation}
C = -\sum_{i,j} \left( B_{ij} \log w_{ij} + (1-B_{ij}) \log(1-w_{ij}) \right).
\label{eq:umap-C-opt}
\end{equation}

\textbf{Tại sao cross-entropy?} Với mỗi cạnh $(i,j)$, ta có hai ``xác suất tồn tại'': $B_{ij}$ (từ dữ liệu gốc) và $w_{ij}$ (từ nhúng). Mỗi giá trị định nghĩa một phân phối Bernoulli (``cạnh tồn tại'' hoặc ``không''). Cross-entropy~\eqref{eq:umap-C-opt} đo \emph{sự khác nhau} giữa 2 phân phối này: 
\begin{itemize}
  \item Số hạng $B_{ij}\log w_{ij}$: phạt khi cạnh mạnh trong không gian gốc ($B_{ij}$ lớn) nhưng yếu trong không gian nhúng ($w_{ij}$ nhỏ)---tức hai điểm \emph{gần nhau bị đặt xa} $\to$ tạo \textbf{lực hút}.
  \item Số hạng $(1-B_{ij})\log(1-w_{ij})$: phạt khi cạnh yếu trong không gian gốc ($B_{ij}$ nhỏ) nhưng mạnh trong không gian nhúng ($w_{ij}$ lớn)---tức hai điểm \emph{xa nhau bị đặt gần} $\to$ tạo \textbf{lực đẩy}.
\end{itemize}
So sánh: t-SNE dùng $\mathrm{KL}(P\|Q) = \sum p_{ij}\log\frac{p_{ij}}{q_{ij}}$, chỉ có số hạng dạng ``hút''---chỉ phạt khi điểm gần bị đặt xa, nhưng \emph{không trực tiếp} phạt khi điểm xa bị đặt gần (lực đẩy của t-SNE đến gián tiếp qua chuẩn hóa $q_{ij}$). UMAP cân bằng cả hai chiều nhờ cross-entropy, giúp giữ cấu trúc toàn cục tốt hơn \cite{mcinnes2018umap}.

Gradient của cross-entropy~\eqref{eq:umap-C-opt} tách thành lực hút (kéo láng giềng gần nhau) và lực đẩy (tách các cặp không láng giềng). Khai triển chi tiết gradient và cơ chế cập nhật SGD xem tại Phụ lục~\ref{app:umap-gradient}. Learning rate $\eta$ giảm tuyến tính qua các epoch (simulated annealing).

\paragraph{Tóm tắt pipeline.}
\begin{enumerate}
  \item Tìm $n$ láng giềng $\to$ tính $\rho_i$, $\sigma_i$ $\to$ trọng số mờ $v_{j|i}$.
  \item Hợp mờ $\to$ ma trận kề toàn cục $\mathbf{B}$.
  \item Khởi tạo $\mathbf{Y}$ bằng spectral embedding trên $\mathbf{B}$.
  \item SGD: lực hút giữ láng giềng gần, lực đẩy tách các cụm xa.
\end{enumerate}

\paragraph{So sánh t-SNE và UMAP.}
Cùng mục tiêu trực quan hóa nhưng khác mô hình: t-SNE so khớp $p_{ij}$ và $q_{ij}$ bằng $\mathrm{KL}(P\|Q)$; UMAP so khớp $B_{ij}$ và $w_{ij}$ bằng $C$ trên fuzzy set. UMAP thường nhanh hơn trên dữ liệu lớn (SGD cạnh thưa) và thường giữ cấu trúc toàn cục tốt hơn; t-SNE thường tách cụm cục bộ rõ hơn \cite{mcinnes2018umap,vandermaaten2008tsne}. Bảng~\ref{tab:dr-compare} tóm tắt toàn bộ các hướng giảm chiều trong chương.

\begin{table}[H]
\centering
\caption{Các hướng giảm chiều trong chương}
\label{tab:dr-compare}
\begin{tabular}{@{}llll@{}}
\toprule
Phương pháp & Tính chất & Ma trận cốt lõi & Giữ gì \\
\midrule
PCA & Tuyến tính & $\mathbf{K} = \mathbf{X}^\top\mathbf{X}$ & Phương sai toàn cục \\
KPCA & Phi tuyến (kernel) & $\mathbf{K}$ tùy chọn & Cấu trúc kernel \\
Isomap & Manifold & $\mathbf{K}_{\mathrm{Iso}}$ từ geodesic & Khoảng cách toàn cục (xấp xỉ) \\
Laplacian & Manifold & $\mathbf{L}$, $\mathbf{K}_L$ & Láng giềng cục bộ \\
LLE & Manifold & $\mathbf{W}$, $\mathbf{M}$ & Tái tạo tuyến tính cục bộ \\
t-SNE & Phi tuyến & $p_{ij}$, $q_{ij}$ (KL) & Cụm cục bộ, trực quan \\
UMAP & Phi tuyến & $B_{ij}$, $w_{ij}$ (CE) & Cục bộ + toàn cục (tương đối) \\
Chiếu ngẫu nhiên & Ngẫu nhiên & $\frac{\mathbf{A}}{\sqrt{k}}$ & Khoảng cách cặp (JL) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Bổ đề Johnson--Lindenstrauss}
\label{sec:jl}

Khác với PCA (tối ưu phương sai), bổ đề Johnson--Lindenstrauss (JL) trả lời câu hỏi: với $m$ điểm cố định trong $\R^N$, có thể chiếu tuyến tính xuống $\R^k$ với $k$ nhỏ đến mức nào mà \emph{mọi} khoảng cách cặp chỉ bị méo hệ số $(1 \pm \varepsilon)$? 

\begin{lemma}[Tập trung phân phối $\chi^2$]
\label{lem:chi2-conc}
Với $\varepsilon \in (0,1)$ và $Z_1,\ldots,Z_k$ i.i.d. $\mathcal{N}(0,1)$, đặt $S = \sum_{i=1}^k Z_i^2$. Khi đó
\[
\Pr\!\left(\left|\frac{S}{k} - 1\right| > \varepsilon\right) \leq 2 e^{-\frac{\varepsilon^2 k}{4}}.
\]
\end{lemma}

Chứng minh dùng bất đẳng thức Chernoff cho MGF của phân phối $\chi^2$; xem chi tiết tại Phụ lục~\ref{app:chi2-proof}.

\begin{theorem}[Johnson--Lindenstrauss]
\label{lem:jl}
Cho $0 < \varepsilon < 1$ và tập hữu hạn $\mathcal{V} = \{\mathbf{v}_1, \ldots, \mathbf{v}_m\} \subset \R^N$. Nếu $k > \frac{8 \log m}{\varepsilon^2}$, thì tồn tại ánh xạ $f : \R^N \to \R^k$ sao cho mọi $\mathbf{u}, \mathbf{v} \in \mathcal{V}$,
\[
(1-\varepsilon)\|\mathbf{u} - \mathbf{v}\|^2
\leq \|f(\mathbf{u}) - f(\mathbf{v})\|^2
\leq (1+\varepsilon)\|\mathbf{u} - \mathbf{v}\|^2.
\]
\end{theorem}

\begin{proof}
Xét một ma trận ngẫu nhiên $\mathbf{R} \in \R^{k \times N}$ với $R_{ij} \stackrel{\text{i.i.d.}}{\sim} \mathcal{N}(0,1)$. Các hàng $\mathbf{r}_i^\top$ của $\mathbf{R}$ là vector Gaussian độc lập trong $\R^N$. 
Đặt $f(\mathbf{x}) = \frac{1}{\sqrt{k}}\,\mathbf{R}\mathbf{x}$, ta sẽ chứng minh tồn tại ánh xạ $f$ như vậy thỏa mãn bổ đề.  

Với $\mathbf{x} \in \R^N$ bất kỳ, viết $\mathbf{R}\mathbf{x} = (\mathbf{r}_1^\top\mathbf{x}, \ldots, \mathbf{r}_k^\top\mathbf{x})^\top$, nên
\[
\|\mathbf{R}\mathbf{x}\|^2 = \sum_{i=1}^k (\mathbf{r}_i^\top \mathbf{x})^2.
\]
Vì $\mathbf{r}_i^\top \sim \mathcal{N}(\mathbf{0}, \mathbf{I}_N)$ nên $\mathbf{r}_i^\top\mathbf{x} \sim \mathcal{N}(0, \|\mathbf{x}\|^2)$, tức $\mathbf{r}_i^\top\mathbf{x} = \|\mathbf{x}\|\, Z_i$ với $Z_i \stackrel{\text{i.i.d.}}{\sim} \mathcal{N}(0,1)$. Đặt $S = \sum_{i=1}^k Z_i^2 \sim \chi^2_k$, ta có
\[
\frac{\|\mathbf{R}\mathbf{x}\|^2}{\|\mathbf{x}\|^2} = S \Rightarrow
\|f(\mathbf{x})\|^2 = \frac{\|\mathbf{R}\mathbf{x}\|^2}{k} = \frac{S}{k}\,\|\mathbf{x}\|^2.
\]

Áp dụng Bổ đề~\ref{lem:chi2-conc} với $S = \frac{1}{\|\mathbf{x}\|^2}\|\mathbf{R}\mathbf{x}\|^2$ suy ra
\[
\Pr\!\left(\left|\frac{S}{k} - 1\right| > \varepsilon\right) \leq 2 e^{-\frac{\varepsilon^2 k}{4}}.
\]

Đặt $\mathbf{x} = \mathbf{u} - \mathbf{v}$. Vì $f$ tuyến tính, $f(\mathbf{u}) - f(\mathbf{v}) = f(\mathbf{u}-\mathbf{v})$, nên
\[
\Pr\!\left(
(1-\varepsilon)\|\mathbf{u}-\mathbf{v}\|^2
\leq \|f(\mathbf{u}) - f(\mathbf{v})\|^2
\leq (1+\varepsilon)\|\mathbf{u}-\mathbf{v}\|^2
\right) \geq 1 - 2 e^{-\frac{\varepsilon^2 k}{4}}.
\]

Gọi $A_{\mathbf{u},\mathbf{v}}$ là sự kiện khoảng cách cặp $(\mathbf{u},\mathbf{v})$ được bảo toàn (bất đẳng thức trên), và $A = \bigcap_{\mathbf{u} \neq \mathbf{v}} A_{\mathbf{u},\mathbf{v}}$. Với $\binom{m}{2}$ cặp,
\[
\Pr(A^c) = \Pr\!\left(\bigcup_{\mathbf{u} \neq \mathbf{v}} A_{\mathbf{u},\mathbf{v}}^c\right)
\leq \binom{m}{2} \cdot 2 e^{-\frac{\varepsilon^2 k}{4}}
< m^2 e^{-\frac{\varepsilon^2 k}{4}}.
\]
Để $\Pr(A) > 0$ (tức tồn tại $\mathbf{R}$ thỏa mọi cặp), cần $m^2 e^{-\frac{\varepsilon^2 k}{4}} < 1$, tương đương
\[
k > \frac{4 \cdot 2 \log m}{\varepsilon^2} = \frac{8 \log m}{\varepsilon^2}.
\]
Vậy với $k$ như giả thiết định lý, xác suất chọn được $\mathbf{R}$ phù hợp là dương, nên ánh xạ $f$ như vậy tồn tại.
\end{proof}

\paragraph{Ý nghĩa.}
JL cho phép giảm $N$ xuống $k \approx \log m$ (bỏ qua hệ số $\frac{1}{\varepsilon^2}$) mà vẫn giữ hình học cặp điểm---cơ sở cho \emph{random projection} làm tiền xử lý nhanh, không cần SVD. Trong sách \cite{mohri2018foundations} dùng hằng số $20$ thay cho $8$; điều kiện $k > \frac{8\log m}{\varepsilon^2}$ ở trên là phiên bản tinh gọn từ lập luận union bound ở Bước~4.

\begin{example}
Với $m = 1000$, $\varepsilon = 0{,}1$, ngưỡng $k > \frac{8\ln(1000)}{0{,}01} \approx 5{,}5 \times 10^3$. Trên thực nghiệm, random projection có thể được dùng như một baseline nhanh để so sánh với PCA. Tuy nhiên, trong báo cáo này phần thực nghiệm chính tập trung vào PCA, KPCA và Isomap; LLE, Laplacian Eigenmaps, t-SNE và UMAP được ghi nhận như các đối chiếu bổ sung trong notebook.
\end{example}

\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=0.95]
  \draw[fill=blue!10] (-0.3,0) rectangle (4.3,3.2);
  \node at (2,2.8) {\small $\R^N$};
  \foreach \x/\y in {0.5/0.6, 1.2/1.5, 2/0.8, 3/2, 3.5/1} {
    \fill[blue!60] (\x,\y) circle (2pt);
  }
  \draw[-{Stealth}, thick] (4.6,1.6) -- node[above, font=\small] {$\frac{\mathbf{A}}{\sqrt{k}}$} (6.4,1.6);
  \draw[fill=green!10] (6.7,0.5) rectangle (9.2,2.7);
  \node at (7.95,2.4) {\small $\R^k$};
  \foreach \x/\y in {7.2/1.2, 7.8/1.8, 8.5/1} {
    \fill[green!60!black] (\x,\y) circle (2pt);
  }
  \node[align=center, font=\small] at (4.9,-0.5) {Méo khoảng cách cặp trong $(1 \pm \varepsilon)$};
\end{tikzpicture}
\caption{Chiếu ngẫu nhiên Johnson--Lindenstrauss: giảm chiều với bảo đảm trên khoảng cách cặp.}
\label{fig:jl-projection}
\end{figure}

\begin{extension}
\textbf{Độ phức tạp (ước lượng).}
SVD đầy đủ $\mathbf{X}$: $O(\min(Nm^2, N^2 m))$. Floyd--Warshall: $O(m^3)$. Eigendecomposition Laplacian thưa: gần tuyến tính theo số cạnh. Chiếu JL: nhân ma trận $O(kNm)$, không cần SVD---phù hợp tiền xử lý nhanh khi chỉ cần giữ khoảng cách cặp.
\end{extension}

\subsection{Mối liên hệ trong chương}

PCA là nền tảng tuyến tính: tối ưu lỗi tái tạo hoặc phương sai. KPCA thay $\mathbf{X}$ bằng $\mathbf{K}$, mở đường cho kernel tùy biến. Isomap, Laplacian, LLE \emph{thiết kế} $\mathbf{K}$ (hoặc ma trận liên hệ) từ geometry đồ thị rồi dùng cùng khung phân rã phổ. Phần \textbf{[MỞ RỘNG]} t-SNE và UMAP (Mục~\ref{sec:extension-viz}) chuyển sang tối ưu phi tuyến trực tiếp trên nhúng, phù hợp trực quan hóa. JL không tìm trục tối ưu mà chứng minh chiếu ngẫu nhiên đủ tốt---hữu ích khi ưu tiên tốc độ và bảo đảm đơn giản.

Các mảng kiến thức liên quan trong học máy: phương pháp kernel (ánh xạ ngầm $\Phi$), đại số ma trận (SVD, trace, PSD), và học có giám sát có thể dùng $\mathbf{Y}$ làm đầu vào cho mô hình sau. Tài liệu gốc \cite{mohri2018foundations} trình bày đầy đủ hơn; báo cáo này tập trung diễn giải và chứng minh các mắt xích chính.

\FloatBarrier
